# Edge probes on which ASM6x and asm6x still differ, on purpose - each is asm6x's own
# failure, not a form of the language. recheck.sh counts these as known, not as disagreements.
#
# The five reach probes (109, 111-114) were listed here as "no verdict" - asm6x's 4 MB objects
# and 48 MB listings had not come back from the box. Fetched 2026-09-19 evening: all five
# identical, with 444-446 added at the true boundaries (BNOP 2047 words, B -2^20 words from
# the fetch packet's base); 112 and 114 renamed, since neither was over its field after all.
# asm6x warns "relocation template 275 not found; returning OFR_INVALID" and writes no usable
# object for a difference of two labels it has not both placed yet, or of two externals;
# ASM6x settles a same-section difference on a later pass and refuses one across sections.
# The six settled ones are held to TI's word another way since 2026-09-19 evening: dis6x
# reads ASM6x's objects and shows the settled values, and lnk6x links each - labeldiff/.
147-mem-offset-label-diff
276-expr-labels-fwd
280-expr-two-ext-diff
293-expr-label-diff-in-mvk
306-set-of-label-diff
415-reloc-half-diff
434-reloc-mvkl-same-diff
# asm6x has no % operator: .word 7%3 gives it 0x3f01. ASM6x's % is C's.
268-expr-mod
# cl6x fails with no message on an exidx table holding a .byte; ASM6x writes what was asked.
363-sect-exidx-byte-align
# asm6x "accepts" and writes an empty relocation section (421) or drops the instruction with a
# warning (430); ASM6x refuses both: a scope needs another to differ from, and an operator
# is not an instruction operand.
421-reloc-scope-alone
430-reloc-operator-in-mvkl
